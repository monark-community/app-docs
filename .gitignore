node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# Generated at build time from content/docs by `pnpm generate:search-index`.
public/api/

# Checkout of the source repository, made by the sync workflow (and handy
# locally: `pnpm sync:docs --source .app-source`).
.app-source/

# Doc figures, copied from the source repo by `pnpm sync:docs` alongside the
# MDX. Generated like content/docs is, but binary, so it is not committed :
# the sync (locally or in CI) puts them back before a build.
public/docs-assets/

.env*
.DS_Store
