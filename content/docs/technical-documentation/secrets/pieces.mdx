---
title: "Pieces"
description: ""
order: 1
source: "docs/technical-documentation/secrets/pieces.md"
---

# Pieces

| Piece                 | Where                                                                                                  | What                                                                                                                                                      |
| --------------------- | ------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Crypto primitive      | [`@monark/common/crypto`](https://github.com/monark-community/app/blob/develop/packages/common/src/crypto.ts)                                         | Generic AES-256-GCM (`encrypt` / `decrypt` / `loadEncryptionKey`). Shared by TOTP + secrets, each keyed by its own env var.                               |
| Secret store          | [`@monark/secrets`](https://github.com/monark-community/app/blob/develop/packages/secrets/README.md)                                                  | The `Secret` Prisma model, the data layer, RBAC (`secrets.{read,manage}`), events (`secrets.{created,updated,deleted}`), and the write-only tRPC router.  |
| Node read path        | [`ctx.getSecret`](https://github.com/monark-community/app/blob/develop/packages/automation/src/server/registry.ts)                                    | Added to `NodeExecutionContext`; wired in [`engine.ts`](https://github.com/monark-community/app/blob/develop/packages/automation/src/server/engine.ts) to `getSecretValue(org, name)`.                   |
| `secret` config field | [`AutomationNodeConfigFieldType`](https://github.com/monark-community/app/blob/develop/packages/automation/src/contracts/nodes.ts)                    | A node config field whose stored value is the secret's **name** (a reference), never the value.                                                           |
| Editor picker         | [`automation-editor.tsx`](https://github.com/monark-community/app/blob/develop/docs/technical-documentation/services/web/src/app/(authed)/automation/[id]/automation-editor.tsx>) | Renders a `secret` field as a name dropdown (backed by `automation.secrets.list`), empty-state links to `/admin/secrets`.                                 |
| Guarded fetch         | [`@monark/common/http`](https://github.com/monark-community/app/blob/develop/packages/common/src/http.ts)                                             | `assertOutboundUrlSafe(url)` + `safeFetch(url, opts)` ; the scheme/host guard + abort-timeout wrapper reused by the webhook node and the webhooks module. |
