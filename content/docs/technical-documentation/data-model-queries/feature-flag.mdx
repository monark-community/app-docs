---
title: "Feature flag"
description: "The whole language is gated behind data-models.query-language (default off). Off, the record list falls back to the classic filter menu."
order: 6
source: "docs/technical-documentation/data-model-queries/feature-flag.md"
---

# Feature flag

The whole language is gated behind `data-models.query-language` (default **off**). Off,
the record list falls back to the classic filter menu.
