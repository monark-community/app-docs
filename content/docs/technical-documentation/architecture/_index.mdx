---
title: "Architecture & Project Structure"
description: "How the Monark App codebase is organized : the two-tier module system, the package boundaries and how modules communicate, and the monorepo layout + tooling."
order: 0
source: "docs/technical-documentation/architecture/_index.md"
---

# Architecture & Project Structure

How the Monark App codebase is organized : the two-tier module system, the package boundaries and how modules communicate, and the monorepo layout + tooling.

Based on the shipped Phase 0 specification ; see [features-planning/phase-0/modular-architecture.md](https://github.com/monark-community/app/blob/develop/docs/features-planning/phase-0/modular-architecture.md) and [project-scaffolding.md](https://github.com/monark-community/app/blob/develop/docs/features-planning/phase-0/project-scaffolding.md) for the full design rationale. For the **authoritative as-is** module inventory (14 core + 2 extended), boot sequence, and per-capability detail, see [platform-overview.md](https://github.com/monark-community/app/blob/develop/docs/technical-documentation/platform-overview/_index.md) ; for the extension contract ("can a feature ship without touching core?"), see [extensibility-contract.md](https://github.com/monark-community/app/blob/develop/docs/technical-documentation/extensibility-contract/_index.md).

## In this section

- **[Two-tier module system](https://github.com/monark-community/app/blob/develop/docs/technical-documentation/architecture/two-tier-module-system.md)**
- **[Directory tree](https://github.com/monark-community/app/blob/develop/docs/technical-documentation/architecture/directory-tree.md)**
- **[Infrastructure packages](https://github.com/monark-community/app/blob/develop/docs/technical-documentation/architecture/infrastructure-packages.md)**
- **[Codegen tools](https://github.com/monark-community/app/blob/develop/docs/technical-documentation/architecture/codegen-tools.md)**
- **[Tech stack](https://github.com/monark-community/app/blob/develop/docs/technical-documentation/architecture/tech-stack.md)**

## Module boundaries

Each module package exposes exactly three subpath exports :

- `/server` ; backend logic (tRPC routers, data access, event handlers). Runs in `services/api`.
- `/client` ; frontend hooks, components, API clients. Runs in `services/web`.
- `/contracts` ; shared types, Zod schemas, domain event definitions. Importable from both sides.

Anything not exported through these three paths is internal and physically unreachable from outside the package. Workspace package boundaries _are_ the module boundaries.

### Enforcement

- **ESLint boundary rule** : `services/web/**` cannot import `@monark/*/server` ; `services/api/**` cannot import `@monark/*/client`.
- **Tier guard** : `pnpm check:tiers` walks the manifest and fails CI if an extended module imports from another extended module.
- **Codegen drift** : `pnpm gen:events --check` / `pnpm gen:routers --check` (and `pnpm gen:schema --check`) fail CI if a module's events, router registrations, or schema fragment are out of sync.


## Communication between modules

### Event bus

Owned by `@monark/common`. Backend-only, in-process, best-effort pub/sub. Modules emit domain events ; other modules subscribe to the events they care about. The event union is generated from each module's `/contracts/events.ts` by `pnpm gen:events`. Extended modules publish events and subscribe to core events ; they never import from each other directly. (Durability is delegated to `@monark/webhooks`, whose wildcard subscriber persists every emit to an at-least-once outbox.)

### Read interfaces

Core modules export query functions (e.g. "get user by id", "check permission", "resolve org membership") from their `/server` entry point. Extended modules consume these for data they need but don't own.


## Services

Two thin runnable services consume the module packages ; neither contains business logic ; they are wiring layers that compose module exports into a running app :

- `services/web` ; Next.js 15 App Router + Tailwind v4. Imports `/client` and `/contracts` from modules.
- `services/api` ; Express 5 + tRPC v11. Imports `/server` and `/contracts` from modules. Hosts the composed tRPC router (auto-generated by `pnpm gen:routers`), plus `/health`, the cron endpoints, and the public REST API (`/api/v1`).


## Module manifest

`modules.manifest.ts` at the repo root is the single source of truth for which modules exist and their tier (core / extended). The codegen tools and the tier-check script both read from it.


## Available scripts

| Script            | What it does                                             |
| ----------------- | -------------------------------------------------------- |
| `pnpm dev`        | Launch web (:3000) + api (:4000) concurrently via Turbo. |
| `pnpm build`      | Topological build across the workspace.                  |
| `pnpm lint`       | ESLint across every package.                             |
| `pnpm typecheck`  | `tsc --noEmit` across every package.                     |
| `pnpm test`       | Vitest across every package.                             |
| `pnpm test:e2e`   | Playwright against running web + api.                    |
| `pnpm db:migrate` | Prisma migrate deploy (via `@monark/db`).                |
| `pnpm db:reset`   | Drop + reseed database.                                  |

The full pre-PR gate is `pnpm gen && pnpm typecheck && pnpm lint && pnpm test && pnpm check:tiers`.


## Adding a new module

```bash
pnpm gen:module <name> --tier core|extended
pnpm install
pnpm gen
```

The generator scaffolds the package with `/server`, `/client`, `/contracts` entry points, registers it in the manifest, and wires it into the codegen pipeline. The tRPC app router, domain-event union, and assembled schema update automatically on `pnpm gen`. Wire the module's `register*` helpers into [`services/api/src/server.ts`](https://github.com/monark-community/app/blob/develop/services/api/src/server.ts), and see [extensibility-contract.md](https://github.com/monark-community/app/blob/develop/docs/technical-documentation/extensibility-contract/_index.md) for the four integration systems (RBAC, events, notifications, feature flags) every module should consider.
